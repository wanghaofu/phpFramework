<?php
namespace Foo\Bar\subnamespace;

const COUNTRY = 'HELLO THI IS UAS';
const FOO = 'is is a changliang in name space Foo\Bar\subnamespace';
function foo() {}
class foo
{
    static function staticmethod() {
    	echo 'i am is foo bar subnamespace';
    }
}
foo::staticmethod();

$unixTime = time();
print_r(getdate($unixTime));


 print date("r", strtotime("+1 week"));
 
$ss = function()
{
	return 'ss';
}
apc_delete_file(newt_grid_add_components_to_form(
	newt_grid_add_components_to_form(

		ncurses_assume_default_colors(fg, bg), form, recurse), form, recurse))
ncurses_assume_default_colors(fg, bg)
switch (do {
	# code...
} while ( <= 10);) {
	case 'value':
		# code...
		break;
	
	default:
		# code...
		break;
}
xattr_get(filename, name)
xattr_get(
	, name)

vfprintf(
	, format, args)
new stats_rand_phrase_to_seeds(
	);
array_intersect(array1, array2);
?>